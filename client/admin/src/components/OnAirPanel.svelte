<div class="onair-panel">
  <div class="placeholder">ON AIR — à venir</div>
</div>

<style>
  .onair-panel {
    display: flex;
    flex-direction: column;
    background: var(--bg-panel);
    border-bottom: 1px solid var(--border-dim);
  }

  .placeholder {
    padding: 8px 12px;
    font-size: 10px;
    color: var(--text-dim);
    font-family: var(--font-mono);
  }
</style>
